# MCP Track

This folder is for Model Context Protocol (MCP) related work.

Planned contents:

- API shape proposal (`serve_mcp`, `to_mcp_tools`, or equivalent)
- Tool schema for `parse`, `generate`, `validate`, and error explanation
- Minimal server implementation notes
- Integration and evaluation checklist
