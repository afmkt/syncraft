Task: Write Syncraft regex++ grammar code.

Spec:
{{SPEC_TEXT}}

Examples:
{{EXAMPLES_TEXT}}

Output requirements:
- Return Python code only.
- Use `Syntax.rp` as primary style.
- Expose final grammar as variable `grammar`.
- Include short parse demo in `if __name__ == "__main__":` if examples are present.
