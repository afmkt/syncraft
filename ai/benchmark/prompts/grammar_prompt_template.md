# Prompt Template (@grammar variant)

You are implementing this task using Syncraft `@grammar` class style.

- Prefer explicit `@grammar` classes and `rule(...)` roots.
- Use `@lazy` methods for recursion where needed.
- Keep transformations attached to grammar rules.
- Target file(s): {{TARGET_FILES}}
- Test file: {{TEST_FILE}}

Task spec:
{{TASK_SPEC}}

Output requirements:
- Produce working code only.
- Ensure tests pass without changing unrelated code.
