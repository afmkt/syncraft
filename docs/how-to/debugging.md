# Debugging & errors

- Use `.cut()` to commit and avoid backtracking on a branch.
- Pretty-print errors with `syncraft.diagnostic.rich_error`.
- Inspect parser structure via `Syntax.meta.to_string` or `diagnostic.rich_parser`.
